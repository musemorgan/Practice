#include <iostream>
#include <string>

using namespace std;

int main ()
{
int a, b, c;

cout << "Lets evalute (13.2 + 9.76) / 4. The answer is = " << (13.2 + 9.76) / 4 << endl;
cout<<"Also, 15/2 = "<< static_cast<double>(29)/2 <<endl;
//adding paranthesis to the first number makes the answer into a mixed expression which allows for the answer to become a floating point number
    return 0;
}