#include <iostream>
#include <string>


using namespace std;

int main ()
{
int a, b, c;
//char numberType;

cout << "Please enter 3 numbers: " << endl;
cin >> a >> b >> c;

if (a>b) swap (a, b);
if (b>c) swap (b,c);
if (a>b) swap (a,b);
//INCOMPLETE PROGRAM
    return 0;
}